@model Phone

@{
    ViewBag.Title = "Удаление";
    Layout = "_Layout";
}

<h2>Вы уверены что хотите удалить @Model.Name?</h2>
<form asp-action="Delete" asp-route-id="@Model.Id">
    <button class="btn btn-primary"  type="submit">Подтвердит</button>
    <a class="btn btn-primary" asp-action="Index">Отменить</a>
</form>
